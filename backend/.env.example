# Puerto donde escucha Express
PORT=8080

# Identificador de la hoja de cálculo (se toma de la URL de Google Sheets)
SPREADSHEET_ID=TU_SPREADSHEET_ID

# Nombre de la pestaña que contiene los registros
SHEET_NAME=Registros

# Ruta absoluta al archivo JSON de la cuenta de servicio (opcional si usas ADC)
# GOOGLE_APPLICATION_CREDENTIALS=/ruta/a/credenciales.json

# Opcional: tiempo de vida del caché de la hoja en milisegundos (por defecto 60000)
CACHE_TTL_MS=60000
